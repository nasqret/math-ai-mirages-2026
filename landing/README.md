# Landing page

GitHub Pages entry point. `scripts/build_site.sh` assembles this page, the built
Jupyter Book, and the final PowerPoint into one deployable site. The release is
served from the `gh-pages` branch; an optional Actions workflow template lives
under `deployment/`.
