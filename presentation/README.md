# Historical presentation export

This directory records the earlier 37-slide PowerPoint content and QA. The
primary lecture deck is now the browser-native presentation under `landing/`.
The export is retained for provenance but is not assembled into the GitHub Pages
site and is not part of the active validation path.
